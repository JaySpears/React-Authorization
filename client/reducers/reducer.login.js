const initialState = {
  form: {
    email: '',
    password: '',
    firstName: '',
    lastName: ''
  }
};

function loginReducer(state = initialState, action){
  switch (action.type) {
    default:
      return state
  }
}

export default loginReducer;
